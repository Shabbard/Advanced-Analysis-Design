@page "/Patient"
@layout EmptyLayout
@using AdvancedAnalysisDesign.Models.Database
@using AdvancedAnalysisDesign.Services
@using InputType = MudBlazor.InputType

@inject UserService UserService
@inject ISnackbar Snackbar
@inject NavigationManager NavManager
@*Limit to user + admin*@
@*Link to specific user*@

<MudCardHeader>
    <CardHeaderContent>
        @*Add Specific users name*@
        <MudText Align="Align.Center" Typo="Typo.h3">Username</MudText>
    </CardHeaderContent>
</MudCardHeader>
<MudContainer MaxWidth="MaxWidth.Small" Class="mt-16">
    <MudCard Elevation="5" Class="MedicationCard">
        <MudCardHeader>
            <CardHeaderContent>
                <MudText Align="Align.Center" Typo="Typo.h5">Medication</MudText>
            </CardHeaderContent>
        </MudCardHeader>

        @* Data table for Medication *@
        @*@<MudTable Items="usermedication.get" Hover="@hover" Breakpoint="Breakpoint.Sm">
            <HeaderContent>
                <MudTh>Name</MudTh>
                <MudTh>Expiry</MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd DataLabel="Name">@context.Name</MudTd>
                <MudTd DataLabel="Expiry">@context.Expiry</MudTd>
            </RowTemplate>
            <PagerContent>
                <MudTablePager />
            </PagerContent>
         </MudTable>*@

    </MudCard>
</MudContainer>
<MudContainer MaxWidth="MaxWidth.Small" Class="mt-16">
    <MudCard Elevation="5" Class="BloodworkCard">
        <MudCardHeader>
            <CardHeaderContent>
                <MudText Align="Align.Center" Typo="Typo.h5">Bloodwork</MudText>
            </CardHeaderContent>
        </MudCardHeader>

        @* Data table for Bloodwork *@
        @*@<MudTable Items="userbloodwork.get" Hover="@hover" Breakpoint="Breakpoint.Sm">
            <HeaderContent>
               <MudTh>Type</MudTh>
               <MudTh>Date</MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd DataLabel="Type">@context.Type</MudTd>
                <MudTd DataLabel="Date">@context.Date</MudTd>
            </RowTemplate>
            <PagerContent>
                <MudTablePager />
            </PagerContent>
        </MudTable>*@

    </MudCard>
</MudContainer>

@code{
    private bool hover = true;

    @*Add authorisation*@
}